@startuml

package "{{ object }}" #AliceBlue {
    {% for module in object.modules.all %}
    [{{ module.name }}]
    {% for interface in module.interfaces.all %}
    () "{{ interface }}"
    {% endfor %}
    {% endfor %}
}

{% for module in object.modules.all %}

{% for interface in module.interfaces.all %}
[{{ module.name }}] -- () "{{ interface }}"
{% endfor %}

{% for interface in module.dependencies.all %}
() "{{ interface }}" <-- [{{ module.name }}]: use
{% endfor %}

{% endfor %}

@enduml
