@startuml {{ cache_base }}/system_components_diagram_{{ object.pk }}.png
{% for module in object.modules.all %}

[{{ module.name }}]

{% for interface in module.interfaces.all %}
[{{ module.name }}] -- () "{{ interface.name }}"
{% endfor %}

{% for interface in module.dependencies.all %}
() "{{ interface.name }}" <-- [{{ module.name }}]: use
{% endfor %}

{% endfor %}
@enduml
